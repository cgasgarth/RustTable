[package]
name = "rusttable-image-io"
description = "Bounded JPEG, PNG, and classic-TIFF file input/output for RustTable."
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
image = { version = "0.25.8", default-features = false, features = ["jpeg", "png", "tiff"] }
rusttable-image = { path = "../rusttable-image", version = "0.1.0" }

[lints]
workspace = true
