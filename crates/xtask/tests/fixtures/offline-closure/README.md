These fixtures describe failure modes that the offline source-closure contract must reject.
They are intentionally inert metadata; the tests construct the temporary vendor trees so no
registry, Git checkout, or network access is needed by fast validation.
