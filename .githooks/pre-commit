#!/usr/bin/env bash
set -euo pipefail

bash "$(git rev-parse --show-toplevel)/.githooks/pre-commit-guard"

if command -v pre-commit >/dev/null 2>&1; then
  exec pre-commit run --hook-stage pre-commit "$@"
fi

printf 'pre-commit is not installed; protected-branch guard passed.\n' >&2
